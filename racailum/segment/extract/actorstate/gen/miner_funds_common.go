// ATTENTION!
// THIS FILE IS GENERATED BY tool/genvercodes/main.go
// DO NOT EDIT MANUALLY

package gen

import (
	"fmt"

	"github.com/filecoin-project/go-bitfield"
	"github.com/filecoin-project/go-state-types/abi"
	"github.com/filecoin-project/go-state-types/big"
	"github.com/ipfs/go-cid"

	miner0 "github.com/filecoin-project/specs-actors/actors/builtin/miner"
	adt0 "github.com/filecoin-project/specs-actors/actors/util/adt"

	miner2 "github.com/filecoin-project/specs-actors/v2/actors/builtin/miner"
	adt2 "github.com/filecoin-project/specs-actors/v2/actors/util/adt"

	miner3 "github.com/filecoin-project/specs-actors/v3/actors/builtin/miner"
	adt3 "github.com/filecoin-project/specs-actors/v3/actors/util/adt"

	miner4 "github.com/filecoin-project/specs-actors/v4/actors/builtin/miner"
	adt4 "github.com/filecoin-project/specs-actors/v4/actors/util/adt"

	miner5 "github.com/filecoin-project/specs-actors/v5/actors/builtin/miner"
	adt5 "github.com/filecoin-project/specs-actors/v5/actors/util/adt"

	miner6 "github.com/filecoin-project/specs-actors/v6/actors/builtin/miner"
	adt6 "github.com/filecoin-project/specs-actors/v6/actors/util/adt"

	miner7 "github.com/filecoin-project/specs-actors/v7/actors/builtin/miner"
	adt7 "github.com/filecoin-project/specs-actors/v7/actors/util/adt"

	miner8 "github.com/filecoin-project/go-state-types/builtin/v8/miner"
	adt8 "github.com/filecoin-project/go-state-types/builtin/v8/util/adt"

	miner9 "github.com/filecoin-project/go-state-types/builtin/v9/miner"
	adt9 "github.com/filecoin-project/go-state-types/builtin/v9/util/adt"

	miner10 "github.com/filecoin-project/go-state-types/builtin/v10/miner"
	adt10 "github.com/filecoin-project/go-state-types/builtin/v10/util/adt"

	miner11 "github.com/filecoin-project/go-state-types/builtin/v11/miner"
	adt11 "github.com/filecoin-project/go-state-types/builtin/v11/util/adt"

	miner12 "github.com/filecoin-project/go-state-types/builtin/v12/miner"
	adt12 "github.com/filecoin-project/go-state-types/builtin/v12/util/adt"

	miner13 "github.com/filecoin-project/go-state-types/builtin/v13/miner"
	adt13 "github.com/filecoin-project/go-state-types/builtin/v13/util/adt"

	miner14 "github.com/filecoin-project/go-state-types/builtin/v14/miner"
	adt14 "github.com/filecoin-project/go-state-types/builtin/v14/util/adt"

	miner15 "github.com/filecoin-project/go-state-types/builtin/v15/miner"
	adt15 "github.com/filecoin-project/go-state-types/builtin/v15/util/adt"

	miner16 "github.com/filecoin-project/go-state-types/builtin/v16/miner"
	adt16 "github.com/filecoin-project/go-state-types/builtin/v16/util/adt"

	miner17 "github.com/filecoin-project/go-state-types/builtin/v17/miner"
	adt17 "github.com/filecoin-project/go-state-types/builtin/v17/util/adt"

	miner18 "github.com/filecoin-project/go-state-types/builtin/v18/miner"
	adt18 "github.com/filecoin-project/go-state-types/builtin/v18/util/adt"

	"github.com/filecoin-project/go-state-types/builtin"

	builtin3 "github.com/filecoin-project/specs-actors/v3/actors/builtin"

	builtin4 "github.com/filecoin-project/specs-actors/v4/actors/builtin"

	builtin5 "github.com/filecoin-project/specs-actors/v5/actors/builtin"

	builtin6 "github.com/filecoin-project/specs-actors/v6/actors/builtin"

	builtin7 "github.com/filecoin-project/specs-actors/v7/actors/builtin"

	"github.com/ipfs-force-community/londobell/common"
	"github.com/ipfs-force-community/londobell/lib/mir"
	"github.com/ipfs-force-community/londobell/racailum/segment/extract"
	"github.com/ipfs-force-community/londobell/racailum/segment/model"
	"github.com/ipfs-force-community/londobell/racailum/segment/model/schema"
)

func init() {
	schema.Register(
		schema.Model{
			Name: "miner-funds",
			D:    &model.MinerFunds{},
		},
	)
}

func NewTokenAmountArr(length int) []abi.TokenAmount {
	s := make([]abi.TokenAmount, length, length)
	for i := range s {
		s[i] = abi.NewTokenAmount(0)
	}
	return s
}

func extractMinerFunds(ctx *extract.Ctx, res *extract.Res, head *common.ActorHead, st interface{}) error {
	tsRange := NormalEpochRange(head)
	vestInFuture := NewTokenAmountArr(len(tsRange))
	pledgeRelease := NewTokenAmountArr(len(tsRange))

	var detail model.MinerFundsDetail
	var mInfo = model.MinerInfo{}
	sum := abi.NewTokenAmount(0)

	switch st := st.(type) {
	case *miner0.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner2.State: %w", err)
		}

		if !st.VestingFunds.Equals(emptyMinerStateV0.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt0.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			for _, v := range funds.Funds {
				sum = big.Add(sum, v.Amount)
			}
		}

		info, err := st.GetInfo(adt0.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance = st.GetAvailableBalance(head.Balance)

		precommitted, err := adt0.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors)

		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner0.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.State = st

	case *miner2.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner2.State: %w", err)
		}

		if !st.VestingFunds.Equals(emptyMinerStateV2.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt2.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			for _, v := range funds.Funds {
				sum = big.Add(sum, v.Amount)
			}
		}

		info, err := st.GetInfo(adt2.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors

		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress
		mInfo.FeeDebt = st.FeeDebt

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		precommitted, err := adt2.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors)

		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner2.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.State = st

	case *miner3.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner2.State: %w", err)
		}

		if !st.VestingFunds.Equals(emptyMinerStateV3.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt3.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			for _, v := range funds.Funds {
				sum = big.Add(sum, v.Amount)
			}
		}

		info, err := st.GetInfo(adt3.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors

		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress
		mInfo.FeeDebt = st.FeeDebt

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		precommitted, err := adt3.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin3.DefaultHamtBitwidth)

		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner3.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.State = st

	case *miner4.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner2.State: %w", err)
		}

		if !st.VestingFunds.Equals(emptyMinerStateV4.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt4.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			for _, v := range funds.Funds {
				sum = big.Add(sum, v.Amount)
			}
		}

		info, err := st.GetInfo(adt4.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors

		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress
		mInfo.FeeDebt = st.FeeDebt

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		precommitted, err := adt4.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin4.DefaultHamtBitwidth)

		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner4.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.State = st

	case *miner5.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner5.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		if !st.VestingFunds.Equals(emptyMinerStateV5.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt5.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			// assign vest in future
			for _, v := range funds.Funds {
				if v.Epoch < head.Epoch {
					continue
				}

				for j := range tsRange {
					if v.Epoch < tsRange[j] {
						vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
						break
					}
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner5.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner5.Partition
			dlInfo := miner5.NewDeadlineInfo(st.CurrentProvingPeriodStart(head.Epoch), dlIdx, head.Epoch).NextNotElapsed()
			quant := miner5.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner5.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner5.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := expirations.PopUntil(tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt5.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt5.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin5.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner5.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.State = st

	case *miner6.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner6.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		if !st.VestingFunds.Equals(emptyMinerStateV6.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt6.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			// assign vest in future
			for _, v := range funds.Funds {
				if v.Epoch < head.Epoch {
					continue
				}

				for j := range tsRange {
					if v.Epoch < tsRange[j] {
						vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
						break
					}
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner6.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner6.Partition
			dlInfo := miner6.NewDeadlineInfo(st.CurrentProvingPeriodStart(head.Epoch), dlIdx, head.Epoch).NextNotElapsed()
			quant := miner6.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner6.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner6.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := expirations.PopUntil(tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt6.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt6.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin6.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner6.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.State = st

	case *miner7.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner7.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		if !st.VestingFunds.Equals(emptyMinerStateV7.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt7.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			// assign vest in future
			for _, v := range funds.Funds {
				if v.Epoch < head.Epoch {
					continue
				}

				for j := range tsRange {
					if v.Epoch < tsRange[j] {
						vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
						break
					}
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner7.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner7.Partition
			dlInfo := miner7.NewDeadlineInfo(st.CurrentProvingPeriodStart(head.Epoch), dlIdx, head.Epoch).NextNotElapsed()
			quant := miner7.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner7.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner7.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := expirations.PopUntil(tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt7.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt7.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin7.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner7.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.State = st

	case *miner8.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner8.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		if !st.VestingFunds.Equals(emptyMinerStateV8.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt8.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			// assign vest in future
			for _, v := range funds {
				if v.Epoch < head.Epoch {
					continue
				}

				for j := range tsRange {
					if v.Epoch < tsRange[j] {
						vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
						break
					}
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner8.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner8.Partition
			dlInfo := miner8.NewDeadlineInfo(st.ProvingPeriodStart, dlIdx, head.Epoch).NextNotElapsed()
			quant := miner8.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner8.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner8.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := PopUntilV8(expirations, tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt8.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt8.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner8.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.State = st

	case *miner9.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner9.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		if !st.VestingFunds.Equals(emptyMinerStateV9.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt9.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			// assign vest in future
			for _, v := range funds {
				if v.Epoch < head.Epoch {
					continue
				}

				for j := range tsRange {
					if v.Epoch < tsRange[j] {
						vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
						break
					}
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner9.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner9.Partition
			dlInfo := miner9.NewDeadlineInfo(st.ProvingPeriodStart, dlIdx, head.Epoch).NextNotElapsed()
			quant := miner9.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner9.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner9.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := PopUntilV9(expirations, tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt9.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt9.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner9.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.Beneficiary = info.Beneficiary
		mInfo.BeneficiaryTerm.Quota = info.BeneficiaryTerm.Quota
		mInfo.BeneficiaryTerm.UsedQuota = info.BeneficiaryTerm.UsedQuota
		mInfo.BeneficiaryTerm.Expiration = info.BeneficiaryTerm.Expiration
		if mInfo.PendingBeneficiaryTerm != nil {
			mInfo.PendingBeneficiaryTerm.NewBeneficiary = info.PendingBeneficiaryTerm.NewBeneficiary
			mInfo.PendingBeneficiaryTerm.NewQuota = info.PendingBeneficiaryTerm.NewQuota
			mInfo.PendingBeneficiaryTerm.NewExpiration = info.PendingBeneficiaryTerm.NewExpiration
			mInfo.PendingBeneficiaryTerm.ApprovedByBeneficiary = info.PendingBeneficiaryTerm.ApprovedByBeneficiary
			mInfo.PendingBeneficiaryTerm.ApprovedByNominee = info.PendingBeneficiaryTerm.ApprovedByNominee
		}
		mInfo.State = st

	case *miner10.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner10.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		if !st.VestingFunds.Equals(emptyMinerStateV10.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt10.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			// assign vest in future
			for _, v := range funds {
				if v.Epoch < head.Epoch {
					continue
				}

				for j := range tsRange {
					if v.Epoch < tsRange[j] {
						vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
						break
					}
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner10.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner10.Partition
			dlInfo := miner10.NewDeadlineInfo(st.ProvingPeriodStart, dlIdx, head.Epoch).NextNotElapsed()
			quant := miner10.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner10.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner10.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := PopUntilV10(expirations, tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt10.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt10.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner10.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.Beneficiary = info.Beneficiary
		mInfo.BeneficiaryTerm.Quota = info.BeneficiaryTerm.Quota
		mInfo.BeneficiaryTerm.UsedQuota = info.BeneficiaryTerm.UsedQuota
		mInfo.BeneficiaryTerm.Expiration = info.BeneficiaryTerm.Expiration
		if mInfo.PendingBeneficiaryTerm != nil {
			mInfo.PendingBeneficiaryTerm.NewBeneficiary = info.PendingBeneficiaryTerm.NewBeneficiary
			mInfo.PendingBeneficiaryTerm.NewQuota = info.PendingBeneficiaryTerm.NewQuota
			mInfo.PendingBeneficiaryTerm.NewExpiration = info.PendingBeneficiaryTerm.NewExpiration
			mInfo.PendingBeneficiaryTerm.ApprovedByBeneficiary = info.PendingBeneficiaryTerm.ApprovedByBeneficiary
			mInfo.PendingBeneficiaryTerm.ApprovedByNominee = info.PendingBeneficiaryTerm.ApprovedByNominee
		}
		mInfo.State = st

	case *miner11.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner11.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		if !st.VestingFunds.Equals(emptyMinerStateV11.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt11.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			// assign vest in future
			for _, v := range funds {
				if v.Epoch < head.Epoch {
					continue
				}

				for j := range tsRange {
					if v.Epoch < tsRange[j] {
						vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
						break
					}
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner11.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner11.Partition
			dlInfo := miner11.NewDeadlineInfo(st.ProvingPeriodStart, dlIdx, head.Epoch).NextNotElapsed()
			quant := miner11.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner11.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner11.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := PopUntilV11(expirations, tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt11.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt11.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner11.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.Beneficiary = info.Beneficiary
		mInfo.BeneficiaryTerm.Quota = info.BeneficiaryTerm.Quota
		mInfo.BeneficiaryTerm.UsedQuota = info.BeneficiaryTerm.UsedQuota
		mInfo.BeneficiaryTerm.Expiration = info.BeneficiaryTerm.Expiration
		if mInfo.PendingBeneficiaryTerm != nil {
			mInfo.PendingBeneficiaryTerm.NewBeneficiary = info.PendingBeneficiaryTerm.NewBeneficiary
			mInfo.PendingBeneficiaryTerm.NewQuota = info.PendingBeneficiaryTerm.NewQuota
			mInfo.PendingBeneficiaryTerm.NewExpiration = info.PendingBeneficiaryTerm.NewExpiration
			mInfo.PendingBeneficiaryTerm.ApprovedByBeneficiary = info.PendingBeneficiaryTerm.ApprovedByBeneficiary
			mInfo.PendingBeneficiaryTerm.ApprovedByNominee = info.PendingBeneficiaryTerm.ApprovedByNominee
		}
		mInfo.State = st

	case *miner12.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner12.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		if !st.VestingFunds.Equals(emptyMinerStateV12.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt12.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			// assign vest in future
			for _, v := range funds {
				if v.Epoch < head.Epoch {
					continue
				}

				for j := range tsRange {
					if v.Epoch < tsRange[j] {
						vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
						break
					}
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner12.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner12.Partition
			dlInfo := miner12.NewDeadlineInfo(st.ProvingPeriodStart, dlIdx, head.Epoch).NextNotElapsed()
			quant := miner12.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner12.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner12.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := PopUntilV12(expirations, tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt12.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt12.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner12.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.Beneficiary = info.Beneficiary
		mInfo.BeneficiaryTerm.Quota = info.BeneficiaryTerm.Quota
		mInfo.BeneficiaryTerm.UsedQuota = info.BeneficiaryTerm.UsedQuota
		mInfo.BeneficiaryTerm.Expiration = info.BeneficiaryTerm.Expiration
		if mInfo.PendingBeneficiaryTerm != nil {
			mInfo.PendingBeneficiaryTerm.NewBeneficiary = info.PendingBeneficiaryTerm.NewBeneficiary
			mInfo.PendingBeneficiaryTerm.NewQuota = info.PendingBeneficiaryTerm.NewQuota
			mInfo.PendingBeneficiaryTerm.NewExpiration = info.PendingBeneficiaryTerm.NewExpiration
			mInfo.PendingBeneficiaryTerm.ApprovedByBeneficiary = info.PendingBeneficiaryTerm.ApprovedByBeneficiary
			mInfo.PendingBeneficiaryTerm.ApprovedByNominee = info.PendingBeneficiaryTerm.ApprovedByNominee
		}
		mInfo.State = st

	case *miner13.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner13.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		if !st.VestingFunds.Equals(emptyMinerStateV13.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt13.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			// assign vest in future
			for _, v := range funds {
				if v.Epoch < head.Epoch {
					continue
				}

				for j := range tsRange {
					if v.Epoch < tsRange[j] {
						vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
						break
					}
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner13.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner13.Partition
			dlInfo := miner13.NewDeadlineInfo(st.ProvingPeriodStart, dlIdx, head.Epoch).NextNotElapsed()
			quant := miner13.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner13.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner13.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := PopUntilV13(expirations, tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt13.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt13.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner13.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.Beneficiary = info.Beneficiary
		mInfo.BeneficiaryTerm.Quota = info.BeneficiaryTerm.Quota
		mInfo.BeneficiaryTerm.UsedQuota = info.BeneficiaryTerm.UsedQuota
		mInfo.BeneficiaryTerm.Expiration = info.BeneficiaryTerm.Expiration
		if mInfo.PendingBeneficiaryTerm != nil {
			mInfo.PendingBeneficiaryTerm.NewBeneficiary = info.PendingBeneficiaryTerm.NewBeneficiary
			mInfo.PendingBeneficiaryTerm.NewQuota = info.PendingBeneficiaryTerm.NewQuota
			mInfo.PendingBeneficiaryTerm.NewExpiration = info.PendingBeneficiaryTerm.NewExpiration
			mInfo.PendingBeneficiaryTerm.ApprovedByBeneficiary = info.PendingBeneficiaryTerm.ApprovedByBeneficiary
			mInfo.PendingBeneficiaryTerm.ApprovedByNominee = info.PendingBeneficiaryTerm.ApprovedByNominee
		}
		mInfo.State = st

	case *miner14.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner14.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		if !st.VestingFunds.Equals(emptyMinerStateV14.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt14.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			// assign vest in future
			for _, v := range funds {
				if v.Epoch < head.Epoch {
					continue
				}

				for j := range tsRange {
					if v.Epoch < tsRange[j] {
						vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
						break
					}
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner14.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner14.Partition
			dlInfo := miner14.NewDeadlineInfo(st.ProvingPeriodStart, dlIdx, head.Epoch).NextNotElapsed()
			quant := miner14.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner14.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner14.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := PopUntilV14(expirations, tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt14.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt14.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner14.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.Beneficiary = info.Beneficiary
		mInfo.BeneficiaryTerm.Quota = info.BeneficiaryTerm.Quota
		mInfo.BeneficiaryTerm.UsedQuota = info.BeneficiaryTerm.UsedQuota
		mInfo.BeneficiaryTerm.Expiration = info.BeneficiaryTerm.Expiration
		if mInfo.PendingBeneficiaryTerm != nil {
			mInfo.PendingBeneficiaryTerm.NewBeneficiary = info.PendingBeneficiaryTerm.NewBeneficiary
			mInfo.PendingBeneficiaryTerm.NewQuota = info.PendingBeneficiaryTerm.NewQuota
			mInfo.PendingBeneficiaryTerm.NewExpiration = info.PendingBeneficiaryTerm.NewExpiration
			mInfo.PendingBeneficiaryTerm.ApprovedByBeneficiary = info.PendingBeneficiaryTerm.ApprovedByBeneficiary
			mInfo.PendingBeneficiaryTerm.ApprovedByNominee = info.PendingBeneficiaryTerm.ApprovedByNominee
		}
		mInfo.State = st

	case *miner15.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner15.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		if !st.VestingFunds.Equals(emptyMinerStateV15.VestingFunds) {
			funds, err := st.LoadVestingFunds(adt15.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
			if err != nil {
				return fmt.Errorf("load vesting funds: %w", err)
			}

			// assign vest in future
			for _, v := range funds {
				if v.Epoch < head.Epoch {
					continue
				}

				for j := range tsRange {
					if v.Epoch < tsRange[j] {
						vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
						break
					}
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner15.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner15.Partition
			dlInfo := miner15.NewDeadlineInfo(st.ProvingPeriodStart, dlIdx, head.Epoch).NextNotElapsed()
			quant := miner15.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner15.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner15.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := PopUntilV15(expirations, tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt15.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt15.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner15.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.Beneficiary = info.Beneficiary
		mInfo.BeneficiaryTerm.Quota = info.BeneficiaryTerm.Quota
		mInfo.BeneficiaryTerm.UsedQuota = info.BeneficiaryTerm.UsedQuota
		mInfo.BeneficiaryTerm.Expiration = info.BeneficiaryTerm.Expiration
		if mInfo.PendingBeneficiaryTerm != nil {
			mInfo.PendingBeneficiaryTerm.NewBeneficiary = info.PendingBeneficiaryTerm.NewBeneficiary
			mInfo.PendingBeneficiaryTerm.NewQuota = info.PendingBeneficiaryTerm.NewQuota
			mInfo.PendingBeneficiaryTerm.NewExpiration = info.PendingBeneficiaryTerm.NewExpiration
			mInfo.PendingBeneficiaryTerm.ApprovedByBeneficiary = info.PendingBeneficiaryTerm.ApprovedByBeneficiary
			mInfo.PendingBeneficiaryTerm.ApprovedByNominee = info.PendingBeneficiaryTerm.ApprovedByNominee
		}
		mInfo.State = st

	case *miner16.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner16.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		funds, err := st.LoadVestingFunds(adt16.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load vesting funds: %w", err)
		}

		// assign vest in future
		for _, v := range funds {
			if v.Epoch < head.Epoch {
				continue
			}

			for j := range tsRange {
				if v.Epoch < tsRange[j] {
					vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
					break
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner16.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner16.Partition
			dlInfo := miner16.NewDeadlineInfo(st.ProvingPeriodStart, dlIdx, head.Epoch).NextNotElapsed()
			quant := miner16.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner16.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner16.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := PopUntilV16(expirations, tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt16.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt16.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner16.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.Beneficiary = info.Beneficiary
		mInfo.BeneficiaryTerm.Quota = info.BeneficiaryTerm.Quota
		mInfo.BeneficiaryTerm.UsedQuota = info.BeneficiaryTerm.UsedQuota
		mInfo.BeneficiaryTerm.Expiration = info.BeneficiaryTerm.Expiration
		if mInfo.PendingBeneficiaryTerm != nil {
			mInfo.PendingBeneficiaryTerm.NewBeneficiary = info.PendingBeneficiaryTerm.NewBeneficiary
			mInfo.PendingBeneficiaryTerm.NewQuota = info.PendingBeneficiaryTerm.NewQuota
			mInfo.PendingBeneficiaryTerm.NewExpiration = info.PendingBeneficiaryTerm.NewExpiration
			mInfo.PendingBeneficiaryTerm.ApprovedByBeneficiary = info.PendingBeneficiaryTerm.ApprovedByBeneficiary
			mInfo.PendingBeneficiaryTerm.ApprovedByNominee = info.PendingBeneficiaryTerm.ApprovedByNominee
		}
		mInfo.State = st

	case *miner17.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner17.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		funds, err := st.LoadVestingFunds(adt17.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load vesting funds: %w", err)
		}

		// assign vest in future
		for _, v := range funds {
			if v.Epoch < head.Epoch {
				continue
			}

			for j := range tsRange {
				if v.Epoch < tsRange[j] {
					vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
					break
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner17.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner17.Partition
			dlInfo := miner17.NewDeadlineInfo(st.ProvingPeriodStart, dlIdx, head.Epoch).NextNotElapsed()
			quant := miner17.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner17.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner17.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := PopUntilV17(expirations, tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt17.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt17.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner17.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.Beneficiary = info.Beneficiary
		mInfo.BeneficiaryTerm.Quota = info.BeneficiaryTerm.Quota
		mInfo.BeneficiaryTerm.UsedQuota = info.BeneficiaryTerm.UsedQuota
		mInfo.BeneficiaryTerm.Expiration = info.BeneficiaryTerm.Expiration
		if mInfo.PendingBeneficiaryTerm != nil {
			mInfo.PendingBeneficiaryTerm.NewBeneficiary = info.PendingBeneficiaryTerm.NewBeneficiary
			mInfo.PendingBeneficiaryTerm.NewQuota = info.PendingBeneficiaryTerm.NewQuota
			mInfo.PendingBeneficiaryTerm.NewExpiration = info.PendingBeneficiaryTerm.NewExpiration
			mInfo.PendingBeneficiaryTerm.ApprovedByBeneficiary = info.PendingBeneficiaryTerm.ApprovedByBeneficiary
			mInfo.PendingBeneficiaryTerm.ApprovedByNominee = info.PendingBeneficiaryTerm.ApprovedByNominee
		}
		mInfo.State = st

	case *miner18.State:
		if err := mir.Mirror(&detail, st); err != nil {
			return fmt.Errorf("mirroring *miner18.State: %w", err)
		}

		if isEmptyOrZero(detail.PreCommitDeposits) &&
			isEmptyOrZero(detail.LockedFunds) &&
			isEmptyOrZero(detail.FeeDebt) &&
			isEmptyOrZero(detail.InitialPledge) {
			return nil
		}
		funds, err := st.LoadVestingFunds(adt18.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load vesting funds: %w", err)
		}

		// assign vest in future
		for _, v := range funds {
			if v.Epoch < head.Epoch {
				continue
			}

			for j := range tsRange {
				if v.Epoch < tsRange[j] {
					vestInFuture[j] = big.Add(vestInFuture[j], v.Amount)
					break
				}
			}
		}
		actStore := ctx.D.ActorStore(ctx.C)
		deadlines, err := st.LoadDeadlines(actStore)
		if err != nil {
			return fmt.Errorf("load deadlines failed: %w", err)
		}

		err = deadlines.ForEach(actStore, func(dlIdx uint64, dl *miner18.Deadline) error {
			if dl == nil {
				return nil
			}

			ps, err := dl.PartitionsArray(actStore)
			if err != nil {
				return fmt.Errorf("get dl partition failed: %w", err)
			}
			var part miner18.Partition
			dlInfo := miner18.NewDeadlineInfo(st.ProvingPeriodStart, dlIdx, head.Epoch).NextNotElapsed()
			quant := miner18.QuantSpecForDeadline(dlInfo)

			return ps.ForEach(&part, func(partIdx int64) error {
				expirations, err := miner18.LoadExpirationQueue(actStore, part.ExpirationsEpochs, quant, miner18.PartitionExpirationAmtBitwidth)
				if err != nil {
					return fmt.Errorf("failed to load expiration queue: %w", err)
				}

				for i := range tsRange {
					popped, err := PopUntilV18(expirations, tsRange[i])
					if err != nil {
						return fmt.Errorf("failed to pop expiration queue until %d: %w", tsRange[i], err)
					}

					pledgeRelease[i] = popped.OnTimePledge
				}

				return nil
			})
		})

		if err != nil {
			return fmt.Errorf("process sector pledge failed")
		}

		info, err := st.GetInfo(adt18.WrapStore(ctx.C, ctx.D.ActorStore(ctx.C)))
		if err != nil {
			return fmt.Errorf("load miner info: %w", err)
		}

		mInfo.Owner = info.Owner
		mInfo.Worker = info.Worker
		mInfo.ControlAddresses = info.ControlAddresses
		if info.PendingWorkerKey != nil {
			mInfo.PendingWorkerKey.NewWorker = info.PendingWorkerKey.NewWorker
			mInfo.PendingWorkerKey.EffectiveAt = info.PendingWorkerKey.EffectiveAt
		}
		mInfo.PeerID = info.PeerId
		mInfo.Multiaddrs = info.Multiaddrs
		mInfo.WindowPoStProofType = info.WindowPoStProofType
		mInfo.SectorSize = info.SectorSize
		mInfo.WindowPoStPartitionSectors = info.WindowPoStPartitionSectors
		mInfo.ConsensusFaultElapsed = info.ConsensusFaultElapsed
		mInfo.PendingOwnerAddress = info.PendingOwnerAddress

		mInfo.Balance = head.Balance
		mInfo.AvailableBalance, err = st.GetAvailableBalance(head.Balance)
		if err != nil {
			return fmt.Errorf("get available balance failed: %w", err)
		}

		mInfo.FeeDebt = st.FeeDebt

		precommitted, err := adt18.AsMap(ctx.D.ActorStore(ctx.C), st.PreCommittedSectors, builtin.DefaultHamtBitwidth)
		if err != nil {
			return fmt.Errorf("load state PreCommittedSectors: %w", err)
		}

		var precommit miner18.SectorPreCommitOnChainInfo
		precommitted.ForEach(&precommit, func(string) error { // nolint: errcheck
			mInfo.PrecommitSectorCount++
			return nil
		})

		mInfo.Beneficiary = info.Beneficiary
		mInfo.BeneficiaryTerm.Quota = info.BeneficiaryTerm.Quota
		mInfo.BeneficiaryTerm.UsedQuota = info.BeneficiaryTerm.UsedQuota
		mInfo.BeneficiaryTerm.Expiration = info.BeneficiaryTerm.Expiration
		if mInfo.PendingBeneficiaryTerm != nil {
			mInfo.PendingBeneficiaryTerm.NewBeneficiary = info.PendingBeneficiaryTerm.NewBeneficiary
			mInfo.PendingBeneficiaryTerm.NewQuota = info.PendingBeneficiaryTerm.NewQuota
			mInfo.PendingBeneficiaryTerm.NewExpiration = info.PendingBeneficiaryTerm.NewExpiration
			mInfo.PendingBeneficiaryTerm.ApprovedByBeneficiary = info.PendingBeneficiaryTerm.ApprovedByBeneficiary
			mInfo.PendingBeneficiaryTerm.ApprovedByNominee = info.PendingBeneficiaryTerm.ApprovedByNominee
		}
		mInfo.State = st

	}

	detail.VestInFuture = vestInFuture
	detail.PledgeRelease = pledgeRelease
	// all zero
	if isEmptyOrZero(detail.PreCommitDeposits) &&
		isEmptyOrZero(detail.LockedFunds) &&
		isEmptyOrZero(detail.FeeDebt) &&
		isEmptyOrZero(detail.InitialPledge) {
		return nil
	}

	id, err := GenRegularHeadID(head.Head, head.Addr, head.Epoch)
	if err != nil {
		return fmt.Errorf("generate regular id: %w", err)
	}

	res.Docs = append(res.Docs, &model.MinerFunds{
		ActorStateExBasic: model.ActorStateExBasic{
			ID:    id,
			Path:  []cid.Cid{head.Head},
			Addr:  head.Addr,
			Epoch: head.Epoch,
		},
		Detail: detail,
		Info:   mInfo,
	})

	return nil
}

func PopUntilV8(q miner8.ExpirationQueue, until abi.ChainEpoch) (*miner8.ExpirationSet, error) {
	var onTimeSectors []bitfield.BitField
	var earlySectors []bitfield.BitField
	activePower := miner8.NewPowerPairZero()
	faultyPower := miner8.NewPowerPairZero()
	onTimePledge := big.Zero()

	var poppedKeys []uint64
	var thisValue miner8.ExpirationSet
	stopErr := fmt.Errorf("stop")
	if err := q.Array.ForEach(&thisValue, func(i int64) error {
		if abi.ChainEpoch(i) > until {
			return stopErr
		}
		poppedKeys = append(poppedKeys, uint64(i))
		onTimeSectors = append(onTimeSectors, thisValue.OnTimeSectors)
		earlySectors = append(earlySectors, thisValue.EarlySectors)
		activePower = activePower.Add(thisValue.ActivePower)
		faultyPower = faultyPower.Add(thisValue.FaultyPower)
		onTimePledge = big.Add(onTimePledge, thisValue.OnTimePledge)
		return nil
	}); err != nil && err != stopErr {
		return nil, err
	}

	if err := q.Array.BatchDelete(poppedKeys, true); err != nil {
		return nil, err
	}

	allOnTime, err := bitfield.MultiMerge(onTimeSectors...)
	if err != nil {
		return nil, err
	}
	allEarly, err := bitfield.MultiMerge(earlySectors...)
	if err != nil {
		return nil, err
	}
	return &miner8.ExpirationSet{
		OnTimeSectors: allOnTime,
		EarlySectors:  allEarly, OnTimePledge: onTimePledge, ActivePower: activePower, FaultyPower: faultyPower}, nil
}

func PopUntilV9(q miner9.ExpirationQueue, until abi.ChainEpoch) (*miner9.ExpirationSet, error) {
	var onTimeSectors []bitfield.BitField
	var earlySectors []bitfield.BitField
	activePower := miner9.NewPowerPairZero()
	faultyPower := miner9.NewPowerPairZero()
	onTimePledge := big.Zero()

	var poppedKeys []uint64
	var thisValue miner9.ExpirationSet
	stopErr := fmt.Errorf("stop")
	if err := q.Array.ForEach(&thisValue, func(i int64) error {
		if abi.ChainEpoch(i) > until {
			return stopErr
		}
		poppedKeys = append(poppedKeys, uint64(i))
		onTimeSectors = append(onTimeSectors, thisValue.OnTimeSectors)
		earlySectors = append(earlySectors, thisValue.EarlySectors)
		activePower = activePower.Add(thisValue.ActivePower)
		faultyPower = faultyPower.Add(thisValue.FaultyPower)
		onTimePledge = big.Add(onTimePledge, thisValue.OnTimePledge)
		return nil
	}); err != nil && err != stopErr {
		return nil, err
	}

	if err := q.Array.BatchDelete(poppedKeys, true); err != nil {
		return nil, err
	}

	allOnTime, err := bitfield.MultiMerge(onTimeSectors...)
	if err != nil {
		return nil, err
	}
	allEarly, err := bitfield.MultiMerge(earlySectors...)
	if err != nil {
		return nil, err
	}
	return &miner9.ExpirationSet{
		OnTimeSectors: allOnTime,
		EarlySectors:  allEarly, OnTimePledge: onTimePledge, ActivePower: activePower, FaultyPower: faultyPower}, nil
}

func PopUntilV10(q miner10.ExpirationQueue, until abi.ChainEpoch) (*miner10.ExpirationSet, error) {
	var onTimeSectors []bitfield.BitField
	var earlySectors []bitfield.BitField
	activePower := miner10.NewPowerPairZero()
	faultyPower := miner10.NewPowerPairZero()
	onTimePledge := big.Zero()

	var poppedKeys []uint64
	var thisValue miner10.ExpirationSet
	stopErr := fmt.Errorf("stop")
	if err := q.Array.ForEach(&thisValue, func(i int64) error {
		if abi.ChainEpoch(i) > until {
			return stopErr
		}
		poppedKeys = append(poppedKeys, uint64(i))
		onTimeSectors = append(onTimeSectors, thisValue.OnTimeSectors)
		earlySectors = append(earlySectors, thisValue.EarlySectors)
		activePower = activePower.Add(thisValue.ActivePower)
		faultyPower = faultyPower.Add(thisValue.FaultyPower)
		onTimePledge = big.Add(onTimePledge, thisValue.OnTimePledge)
		return nil
	}); err != nil && err != stopErr {
		return nil, err
	}

	if err := q.Array.BatchDelete(poppedKeys, true); err != nil {
		return nil, err
	}

	allOnTime, err := bitfield.MultiMerge(onTimeSectors...)
	if err != nil {
		return nil, err
	}
	allEarly, err := bitfield.MultiMerge(earlySectors...)
	if err != nil {
		return nil, err
	}
	return &miner10.ExpirationSet{
		OnTimeSectors: allOnTime,
		EarlySectors:  allEarly, OnTimePledge: onTimePledge, ActivePower: activePower, FaultyPower: faultyPower}, nil
}

func PopUntilV11(q miner11.ExpirationQueue, until abi.ChainEpoch) (*miner11.ExpirationSet, error) {
	var onTimeSectors []bitfield.BitField
	var earlySectors []bitfield.BitField
	activePower := miner11.NewPowerPairZero()
	faultyPower := miner11.NewPowerPairZero()
	onTimePledge := big.Zero()

	var poppedKeys []uint64
	var thisValue miner11.ExpirationSet
	stopErr := fmt.Errorf("stop")
	if err := q.Array.ForEach(&thisValue, func(i int64) error {
		if abi.ChainEpoch(i) > until {
			return stopErr
		}
		poppedKeys = append(poppedKeys, uint64(i))
		onTimeSectors = append(onTimeSectors, thisValue.OnTimeSectors)
		earlySectors = append(earlySectors, thisValue.EarlySectors)
		activePower = activePower.Add(thisValue.ActivePower)
		faultyPower = faultyPower.Add(thisValue.FaultyPower)
		onTimePledge = big.Add(onTimePledge, thisValue.OnTimePledge)
		return nil
	}); err != nil && err != stopErr {
		return nil, err
	}

	if err := q.Array.BatchDelete(poppedKeys, true); err != nil {
		return nil, err
	}

	allOnTime, err := bitfield.MultiMerge(onTimeSectors...)
	if err != nil {
		return nil, err
	}
	allEarly, err := bitfield.MultiMerge(earlySectors...)
	if err != nil {
		return nil, err
	}
	return &miner11.ExpirationSet{
		OnTimeSectors: allOnTime,
		EarlySectors:  allEarly, OnTimePledge: onTimePledge, ActivePower: activePower, FaultyPower: faultyPower}, nil
}

func PopUntilV12(q miner12.ExpirationQueue, until abi.ChainEpoch) (*miner12.ExpirationSet, error) {
	var onTimeSectors []bitfield.BitField
	var earlySectors []bitfield.BitField
	activePower := miner12.NewPowerPairZero()
	faultyPower := miner12.NewPowerPairZero()
	onTimePledge := big.Zero()

	var poppedKeys []uint64
	var thisValue miner12.ExpirationSet
	stopErr := fmt.Errorf("stop")
	if err := q.Array.ForEach(&thisValue, func(i int64) error {
		if abi.ChainEpoch(i) > until {
			return stopErr
		}
		poppedKeys = append(poppedKeys, uint64(i))
		onTimeSectors = append(onTimeSectors, thisValue.OnTimeSectors)
		earlySectors = append(earlySectors, thisValue.EarlySectors)
		activePower = activePower.Add(thisValue.ActivePower)
		faultyPower = faultyPower.Add(thisValue.FaultyPower)
		onTimePledge = big.Add(onTimePledge, thisValue.OnTimePledge)
		return nil
	}); err != nil && err != stopErr {
		return nil, err
	}

	if err := q.Array.BatchDelete(poppedKeys, true); err != nil {
		return nil, err
	}

	allOnTime, err := bitfield.MultiMerge(onTimeSectors...)
	if err != nil {
		return nil, err
	}
	allEarly, err := bitfield.MultiMerge(earlySectors...)
	if err != nil {
		return nil, err
	}
	return &miner12.ExpirationSet{
		OnTimeSectors: allOnTime,
		EarlySectors:  allEarly, OnTimePledge: onTimePledge, ActivePower: activePower, FaultyPower: faultyPower}, nil
}

func PopUntilV13(q miner13.ExpirationQueue, until abi.ChainEpoch) (*miner13.ExpirationSet, error) {
	var onTimeSectors []bitfield.BitField
	var earlySectors []bitfield.BitField
	activePower := miner13.NewPowerPairZero()
	faultyPower := miner13.NewPowerPairZero()
	onTimePledge := big.Zero()

	var poppedKeys []uint64
	var thisValue miner13.ExpirationSet
	stopErr := fmt.Errorf("stop")
	if err := q.Array.ForEach(&thisValue, func(i int64) error {
		if abi.ChainEpoch(i) > until {
			return stopErr
		}
		poppedKeys = append(poppedKeys, uint64(i))
		onTimeSectors = append(onTimeSectors, thisValue.OnTimeSectors)
		earlySectors = append(earlySectors, thisValue.EarlySectors)
		activePower = activePower.Add(thisValue.ActivePower)
		faultyPower = faultyPower.Add(thisValue.FaultyPower)
		onTimePledge = big.Add(onTimePledge, thisValue.OnTimePledge)
		return nil
	}); err != nil && err != stopErr {
		return nil, err
	}

	if err := q.Array.BatchDelete(poppedKeys, true); err != nil {
		return nil, err
	}

	allOnTime, err := bitfield.MultiMerge(onTimeSectors...)
	if err != nil {
		return nil, err
	}
	allEarly, err := bitfield.MultiMerge(earlySectors...)
	if err != nil {
		return nil, err
	}
	return &miner13.ExpirationSet{
		OnTimeSectors: allOnTime,
		EarlySectors:  allEarly, OnTimePledge: onTimePledge, ActivePower: activePower, FaultyPower: faultyPower}, nil
}

func PopUntilV14(q miner14.ExpirationQueue, until abi.ChainEpoch) (*miner14.ExpirationSet, error) {
	var onTimeSectors []bitfield.BitField
	var earlySectors []bitfield.BitField
	activePower := miner14.NewPowerPairZero()
	faultyPower := miner14.NewPowerPairZero()
	onTimePledge := big.Zero()

	var poppedKeys []uint64
	var thisValue miner14.ExpirationSet
	stopErr := fmt.Errorf("stop")
	if err := q.Array.ForEach(&thisValue, func(i int64) error {
		if abi.ChainEpoch(i) > until {
			return stopErr
		}
		poppedKeys = append(poppedKeys, uint64(i))
		onTimeSectors = append(onTimeSectors, thisValue.OnTimeSectors)
		earlySectors = append(earlySectors, thisValue.EarlySectors)
		activePower = activePower.Add(thisValue.ActivePower)
		faultyPower = faultyPower.Add(thisValue.FaultyPower)
		onTimePledge = big.Add(onTimePledge, thisValue.OnTimePledge)
		return nil
	}); err != nil && err != stopErr {
		return nil, err
	}

	if err := q.Array.BatchDelete(poppedKeys, true); err != nil {
		return nil, err
	}

	allOnTime, err := bitfield.MultiMerge(onTimeSectors...)
	if err != nil {
		return nil, err
	}
	allEarly, err := bitfield.MultiMerge(earlySectors...)
	if err != nil {
		return nil, err
	}
	return &miner14.ExpirationSet{
		OnTimeSectors: allOnTime,
		EarlySectors:  allEarly, OnTimePledge: onTimePledge, ActivePower: activePower, FaultyPower: faultyPower}, nil
}

func PopUntilV15(q miner15.ExpirationQueue, until abi.ChainEpoch) (*miner15.ExpirationSet, error) {
	var onTimeSectors []bitfield.BitField
	var earlySectors []bitfield.BitField
	activePower := miner15.NewPowerPairZero()
	faultyPower := miner15.NewPowerPairZero()
	onTimePledge := big.Zero()

	var poppedKeys []uint64
	var thisValue miner15.ExpirationSet
	stopErr := fmt.Errorf("stop")
	if err := q.Array.ForEach(&thisValue, func(i int64) error {
		if abi.ChainEpoch(i) > until {
			return stopErr
		}
		poppedKeys = append(poppedKeys, uint64(i))
		onTimeSectors = append(onTimeSectors, thisValue.OnTimeSectors)
		earlySectors = append(earlySectors, thisValue.EarlySectors)
		activePower = activePower.Add(thisValue.ActivePower)
		faultyPower = faultyPower.Add(thisValue.FaultyPower)
		onTimePledge = big.Add(onTimePledge, thisValue.OnTimePledge)
		return nil
	}); err != nil && err != stopErr {
		return nil, err
	}

	if err := q.Array.BatchDelete(poppedKeys, true); err != nil {
		return nil, err
	}

	allOnTime, err := bitfield.MultiMerge(onTimeSectors...)
	if err != nil {
		return nil, err
	}
	allEarly, err := bitfield.MultiMerge(earlySectors...)
	if err != nil {
		return nil, err
	}
	return &miner15.ExpirationSet{
		OnTimeSectors: allOnTime,
		EarlySectors:  allEarly, OnTimePledge: onTimePledge, ActivePower: activePower, FaultyPower: faultyPower}, nil
}

func PopUntilV16(q miner16.ExpirationQueue, until abi.ChainEpoch) (*miner16.ExpirationSet, error) {
	var onTimeSectors []bitfield.BitField
	var earlySectors []bitfield.BitField
	activePower := miner16.NewPowerPairZero()
	faultyPower := miner16.NewPowerPairZero()
	onTimePledge := big.Zero()

	var poppedKeys []uint64
	var thisValue miner16.ExpirationSet
	stopErr := fmt.Errorf("stop")
	if err := q.Array.ForEach(&thisValue, func(i int64) error {
		if abi.ChainEpoch(i) > until {
			return stopErr
		}
		poppedKeys = append(poppedKeys, uint64(i))
		onTimeSectors = append(onTimeSectors, thisValue.OnTimeSectors)
		earlySectors = append(earlySectors, thisValue.EarlySectors)
		activePower = activePower.Add(thisValue.ActivePower)
		faultyPower = faultyPower.Add(thisValue.FaultyPower)
		onTimePledge = big.Add(onTimePledge, thisValue.OnTimePledge)
		return nil
	}); err != nil && err != stopErr {
		return nil, err
	}

	if err := q.Array.BatchDelete(poppedKeys, true); err != nil {
		return nil, err
	}

	allOnTime, err := bitfield.MultiMerge(onTimeSectors...)
	if err != nil {
		return nil, err
	}
	allEarly, err := bitfield.MultiMerge(earlySectors...)
	if err != nil {
		return nil, err
	}
	return &miner16.ExpirationSet{
		OnTimeSectors: allOnTime,
		EarlySectors:  allEarly, OnTimePledge: onTimePledge, ActivePower: activePower, FaultyPower: faultyPower}, nil
}

func PopUntilV17(q miner17.ExpirationQueue, until abi.ChainEpoch) (*miner17.ExpirationSet, error) {
	var onTimeSectors []bitfield.BitField
	var earlySectors []bitfield.BitField
	activePower := miner17.NewPowerPairZero()
	faultyPower := miner17.NewPowerPairZero()
	onTimePledge := big.Zero()

	var poppedKeys []uint64
	var thisValue miner17.ExpirationSet
	stopErr := fmt.Errorf("stop")
	if err := q.Array.ForEach(&thisValue, func(i int64) error {
		if abi.ChainEpoch(i) > until {
			return stopErr
		}
		poppedKeys = append(poppedKeys, uint64(i))
		onTimeSectors = append(onTimeSectors, thisValue.OnTimeSectors)
		earlySectors = append(earlySectors, thisValue.EarlySectors)
		activePower = activePower.Add(thisValue.ActivePower)
		faultyPower = faultyPower.Add(thisValue.FaultyPower)
		onTimePledge = big.Add(onTimePledge, thisValue.OnTimePledge)
		return nil
	}); err != nil && err != stopErr {
		return nil, err
	}

	if err := q.Array.BatchDelete(poppedKeys, true); err != nil {
		return nil, err
	}

	allOnTime, err := bitfield.MultiMerge(onTimeSectors...)
	if err != nil {
		return nil, err
	}
	allEarly, err := bitfield.MultiMerge(earlySectors...)
	if err != nil {
		return nil, err
	}
	return &miner17.ExpirationSet{
		OnTimeSectors: allOnTime,
		EarlySectors:  allEarly, OnTimePledge: onTimePledge, ActivePower: activePower, FaultyPower: faultyPower}, nil
}

func PopUntilV18(q miner18.ExpirationQueue, until abi.ChainEpoch) (*miner18.ExpirationSet, error) {
	var onTimeSectors []bitfield.BitField
	var earlySectors []bitfield.BitField
	activePower := miner18.NewPowerPairZero()
	faultyPower := miner18.NewPowerPairZero()
	onTimePledge := big.Zero()

	var poppedKeys []uint64
	var thisValue miner18.ExpirationSet
	stopErr := fmt.Errorf("stop")
	if err := q.Array.ForEach(&thisValue, func(i int64) error {
		if abi.ChainEpoch(i) > until {
			return stopErr
		}
		poppedKeys = append(poppedKeys, uint64(i))
		onTimeSectors = append(onTimeSectors, thisValue.OnTimeSectors)
		earlySectors = append(earlySectors, thisValue.EarlySectors)
		activePower = activePower.Add(thisValue.ActivePower)
		faultyPower = faultyPower.Add(thisValue.FaultyPower)
		onTimePledge = big.Add(onTimePledge, thisValue.OnTimePledge)
		return nil
	}); err != nil && err != stopErr {
		return nil, err
	}

	if err := q.Array.BatchDelete(poppedKeys, true); err != nil {
		return nil, err
	}

	allOnTime, err := bitfield.MultiMerge(onTimeSectors...)
	if err != nil {
		return nil, err
	}
	allEarly, err := bitfield.MultiMerge(earlySectors...)
	if err != nil {
		return nil, err
	}
	return &miner18.ExpirationSet{
		OnTimeSectors: allOnTime,
		EarlySectors:  allEarly, OnTimePledge: onTimePledge, ActivePower: activePower, FaultyPower: faultyPower}, nil
}
