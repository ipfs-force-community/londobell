# Londobell 新增 ActorState 提取器的检查项清单
- [ ] 以 go 代码的形式，基于最新版本的 specs-actors 中的目标数据结构和逻辑完成一个特定版本的 extractor 的编写，包括：
  - 编写抽取逻辑
  - 如有必要，拟定新增的 model 类型，包括表名、索引等

- [ ] 在 init 中对新增的 extractor 和 model 进行注册

- [ ] 生成 model example、model doc、indexes 等，确认注册是否生效

- [ ] 在测试环境中，手工执行一段高度上的新增的抽取逻辑，并检视抽取结果

- [ ] 将源码改造成模板，并通过代码生成器生成旧版本的提取代码，并在合适的位置完成各类注册（通常在 v0 对应的文件中）

- [ ] 解决可能出现的，由旧版本数据结构不兼容导致的代码逻辑问题

- [ ] 在测试环境中，选取对应不同版本的 specs-actors 的多个代码段进行测试

- [ ] 整理代码并提交 PR
